import Foundation

// Assuming AppStrings.swift contains `static let all: [String: [String: String]] = [...]`
// But wait, `AppStrings.swift` uses `SwiftUI` so I can't just run it as a standalone script easily unless I compile it with the right flags, or just read it as text.
